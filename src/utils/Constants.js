const token = '';
const baseUrl = 'http://localhost:3000';
const delayResponse = 500;
const cardsPerPage = 1;
const placesTextForms = [
  'место',
  'места',
  'мест',
];

export {
  baseUrl, token, delayResponse, cardsPerPage, placesTextForms,
};
